### NODE-TS

```
Steps to Run
1. npm install :-  this will Install all the required dependencies
2.  npm run test :- This will execute all the test cases
3. npm run start :- This will start the server on Port 3010

Api end Point to view all the records (Paginated) - http://localhost:3010/bmi

Api end Point to view the count of Overweight people http:/localhost:3010/Overweight

** .env file is also included

```
